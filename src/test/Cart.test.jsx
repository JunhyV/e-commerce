import { describe, test } from "vitest";

describe('Cart functionality', () => {
    test('Display cart box')
    test('Add to cart')
    test('remove from cart')
    test('remove all cart')
    test('modify products quantity from cart')
    test('clear cart')
})